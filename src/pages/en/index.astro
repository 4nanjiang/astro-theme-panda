---
import BlogListLayout from '@layouts/BlogListLayout.astro'
import { useLocale } from '@utils/locale'
const { t } = useLocale(Astro.url)
---
<BlogListLayout title={t('nav.posts')} tag="" category="" />